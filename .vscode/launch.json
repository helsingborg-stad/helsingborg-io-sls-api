{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch file",
      "skipFiles": ["<node_internals>/**"],
      "program": "${file}",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
